//! new-transaction
import 0x0.VDF;
import 0x0.Debug;
main() {
  let challenge: vector<u8>;
  let difficult: u64;
  let solution: vector<u8>;
  let re: bool;

  difficult = 10000;
  challenge = h"aa";
  solution = h"aa";

  re = VDF.verify(move(challenge), move(difficult), move(solution));
  assert(move(re)==false, 7);
  return;
}