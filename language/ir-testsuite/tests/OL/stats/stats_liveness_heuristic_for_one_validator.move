import 0x0.Hello;

main() {
  // test that network heuristics.liveness is returning the correct calculation for ONE validator.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
