import 0x0.Hello;

main() {
  // this test checks that the network heuristics.liveness is returning the correct figures for all the epoch.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
