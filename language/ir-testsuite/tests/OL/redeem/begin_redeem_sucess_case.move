import 0x0.Hello;

main() {
  // this test confirms the three happy cases of redeeming a correct VDF, reading and not finding it in storage, and writing storage.
  // 1. test that it calls VDF.verify correctly
  // 2. test that it reads storage to check for VDF blob being in redemption process
  // 3. test that it writes to storage if the above is correct.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
