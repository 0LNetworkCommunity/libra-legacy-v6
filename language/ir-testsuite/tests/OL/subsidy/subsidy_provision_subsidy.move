import 0x0.Hello;

main() {

  // test that Subsidy is minting the max amount and it is stored in this address (which is called at beginning of epoch)
  // 1. test that it's calling minting with *mock* parameters. (unit test)
  // 2. test that it is in fact minting the correct amount of tokens.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
