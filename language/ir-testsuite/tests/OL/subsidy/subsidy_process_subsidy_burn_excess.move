import 0x0.Hello;

main() {
  // checks that the excess of the coins (not owed to validators per protocol) is being burnt.
  // 1. check that it calls the burn method correctly
  // 2. checks that the coins are in fact in the burn wallets

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
