import 0x0.Hello;

main() {

  // transfer rewards to the recipients from consensus
  // transfers are orginarily not permitted between accounts, except in the case of Subsidy module.
  // 1. test that the transfer is possible from subsidy contract only.
  // note, possibly double check that normal transfers are not allowed (we have a test elsewhere)


  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
