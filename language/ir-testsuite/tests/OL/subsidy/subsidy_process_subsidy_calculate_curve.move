import 0x0.Hello;

main() {
  // tests that the subsidy is being calculated correctly with the calculate_curve.
  // 1. test that it's calling minting with upstream parameters with *mocks*. (unit test)
  // 2. test that it's calling minting with upstream parameters from Stats. (functional test)
  // 3. checks returning correct numbers on the curve.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
