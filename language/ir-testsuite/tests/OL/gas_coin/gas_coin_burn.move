import 0x0.Hello;

main() {

  // This test checks that funds are burnt.
  // 1. Burn is processed
  // 2. Check the state of the contracts to confirm the coins are missing from original location.
  // 3. Check the state that the coins are in the burn wallets as intended.

  let x : bool;
  // x = Hello.hi();
  assert(true==true, 7);
  return;
}
