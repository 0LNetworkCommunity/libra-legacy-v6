{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
        {
          "type": "shell",
          "command": "cargo r -p ol -- --swarm-path ~/swarm_temp --swarm-persona alice init --source-path ~/libra",
          "problemMatcher": [],
          "label": "swarmInit"
        },
  ]
}